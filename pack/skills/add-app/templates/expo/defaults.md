# Expo — add-app 預設功能

手機 app。僅在使用者確認採用預設時實作下列項目；auth client、API 串接、導覽、Maestro 不做。

## 預設項目

| 項目 | 說明 |
|------|------|
| 單一畫面 | 顯示 app 名稱 + placeholder（例如 "t42-starter / {slug}"） |
| `expo doctor` | 整合後執行一次，有問題則修 |

## 依賴

`catalogs.mobile`；**不**預設加 `@t42/auth`
