# Electron — add-app 預設功能

桌面 app。僅在使用者確認採用預設時實作下列項目；自動更新、IPC、web API 串接不做。

## 預設項目

| 項目 | 說明 |
|------|------|
| vanilla-ts 範本 | 保持 degit scaffold 的最小視窗 |
| monorepo 對齊 | `catalog:electron`、`dev` / `typecheck` 可跑 |

## 依賴

`catalogs.electron`
